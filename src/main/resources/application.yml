spring:
  application:
    name: inventory-api
  datasource:
    url: jdbc:postgresql://localhost:5432/inventory_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  # Le schéma est créé et versionné par Flyway (src/main/resources/db/migration).
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration
  jpa:
    # Les contrôleurs renvoient des DTO : aucune session ne doit rester ouverte
    # pendant le rendu de la réponse.
    open-in-view: false
    hibernate:
      # « validate » : Hibernate vérifie que les entités correspondent au schéma
      # migré, sans jamais le modifier lui-même.
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false
  cache:
    type: redis
  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 8080
  servlet:
    context-path: /api

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

logging:
  level:
    root: INFO
    com.kfokam48: DEBUG
